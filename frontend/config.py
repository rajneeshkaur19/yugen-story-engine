from frontend.utils.validators import validate_prompt


class FrontendConfig:
    app_title = "Yugen Story Engine"
    max_prompt_length = 250
    default_prompt = "A quiet shrine at dusk, where memories linger in the mist."


config = FrontendConfig()
